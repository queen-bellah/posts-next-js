/** @type {import('next').NextConfig} */
const nextConfig = {};

// export default nextConfig;
export default {
    images: {
      domains: ['tinyurl.com'], // Add any other domains you want to allow here
    },
  };

